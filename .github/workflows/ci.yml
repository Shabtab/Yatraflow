name: CI

# Issue #21 — this repo previously had no automated gate: the full verification
# (`tsc -b --clean` -> tests -> production build, see AGENTS.md "Verification
# before every push") only ever ran locally/manually, which is how a
# deploy-breaking CSS error (#14) reached main unnoticed. This workflow enforces
# the SAME gate on every PR to main and on every push to main.
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    name: Verify (tsc --clean + tests + build)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          # vite 8 requires Node ^20.19.0 || >=22.12.0; no engines field is set
          # in package.json, so pin the Node LTS that satisfies it.
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run verification gate
        run: npm run verify